---
- hosts: webservers 
  become: true
  tasks:
    - name: Initiate ansible
      debug:
        msg:  "mvn -f /var/lib/jenkins/workspace/ansible-demo1/pom.xml clean install package"
    - name: Initiate ansible
      shell: "sudo mvn -f /var/lib/jenkins/workspace/ansible-demo1/pom.xml clean"
      delegate_to: localhost
...
